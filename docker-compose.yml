version: '3.0'
services:
  server1:
    image: server
    build:
        context: .
        dockerfile: docker/server.development.dockerfile
    command: python manage.py runserver 0.0.0.0:8000
    volumes: ['.:/var/www']
    ports:
      - "8000:8000"